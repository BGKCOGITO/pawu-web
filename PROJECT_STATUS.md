# PAWU PROJECT STATUS

## 현재 버전
V9.6.3 성능 안정화 패치

## 현재 정상 작동하는 기능
- 보호자/병원 로그인
- 예약 및 예약관리
- 보호자/병원 채팅 송수신
- 보호자 앱 종료·잠금 상태 FCM 푸시, 소리·진동
- 앱 내부 알림
- 보호자 반려동물 관리
- 병원 DB 및 관리자 기능
- 채팅 저장 실패와 푸시 실패의 예외 분리

## 최근 완료한 작업
- Firebase 환경변수 8개를 Vercel에 등록
- 보호자 휴대폰에서 앱 종료 상태 채팅 푸시 정상 수신 확인
- 최신 `PAWU.zip` 실제 코드에서 반복 조회 위치 확인
- 채팅방 5초 전체 조회 제거
- 보호자 채팅 배지 15초 조회 제거
- 병원 채팅 목록 및 접수 화면 15초 조회 제거
- Supabase Realtime과 화면 복귀 1회 동기화 방식으로 전환
- 중복 요청 방지 및 메시지 즉시 화면 반영 추가

## 지금 진행 중인 작업
- 사용자 PC에서 V9.6.3 로컬 실행 테스트
- 배포 후 보호자/병원 앱 체감 속도 확인
- Realtime 수신 누락 여부 및 중복 표시 여부 확인

## 발견된 오류와 미해결 문제
- 현재 실행 환경의 npm 사설 저장소에서 `zod-validation-error@4.0.2` 패키지를 찾지 못해 전체 `npm install` 및 빌드를 완료하지 못함
- 병원 대시보드의 60초 자동 갱신은 운영 현황 표시 목적이라 이번 패치에서는 유지
- 복약 알림 화면의 30초 타이머는 로컬 복약 시간 확인 기능이라 이번 패치에서는 유지
- Supabase Realtime 연결이 일시적으로 끊기면 화면 복귀/포커스 시 1회 재조회로 복구하도록 구성됨

## 다음에 진행할 정확한 작업
1. 패치 ZIP을 `C:\Users\USER\pawu-web`에 폴더 구조 그대로 덮어쓰기
2. `npm run dev` 실행
3. 보호자와 병원 계정으로 양방향 채팅 즉시 수신 테스트
4. 메시지 중복 표시 및 채팅방 진입 오류 확인
5. 병원 채팅 목록과 접수 보드 자동 반영 확인
6. 앱 종료 상태 FCM 푸시가 그대로 유지되는지 확인
7. 정상 확인 후 Git 커밋·푸시 및 Vercel 배포
8. 배포 후 휴대폰 앱을 재실행하고 체감 속도 확인

## 마지막으로 수정한 파일 목록
- `components/chat/ConversationRoom.tsx`
- `components/GuardianBottomNav.tsx`
- `app/api/chat/messages/route.ts`
- `app/hospital-admin/chat/page.tsx`
- `app/hospital-admin/reception/page.tsx`
- `PAWU_MASTER.md`
- `PROJECT_STATUS.md`
- `CHANGELOG.md`
- `README-V9.6.3-PERFORMANCE-PATCH.txt`

## 사용자 테스트가 필요한 항목
- 보호자 채팅방 첫 진입
- 병원→보호자 메시지 즉시 표시
- 보호자→병원 메시지 즉시 표시
- 전송한 메시지가 두 번 표시되지 않는지
- 채팅방을 1분 이상 열어도 반복 깜박임이 없는지
- 앱을 백그라운드에 두었을 때 불필요한 반복 로딩이 없는지
- 앱 복귀 후 최신 메시지가 동기화되는지
- 보호자 하단 채팅 배지가 정상 갱신되는지
- 병원 채팅 목록의 최근 메시지와 읽지 않은 수 갱신
- 접수·대기 상태 변경이 보드에 반영되는지
- 앱 종료·잠금 상태 FCM 알림, 소리, 진동 유지
