## V9.8.3 - 클라이언트 요청 캐시 및 중복 호출 최적화
### 추가
- 공통 메모리 TTL 캐시와 진행 중 요청 공유 모듈 `lib/client-query-cache.ts`

### 수정
- 하단 채팅 읽지 않음 API 결과를 15초간 재사용
- 동일한 읽지 않음 API 요청이 동시에 발생하면 하나의 요청만 실행
- 홈 입원 경과 API를 60초, 알림 API를 20초 동안 공유
- 채팅 알림 수신 및 읽음 이벤트 발생 시 관련 캐시를 즉시 무효화

### 기존 기능 영향
- 채팅 메시지, FCM 푸시, 예약, 병원 지도, Supabase 스키마는 변경하지 않음
- 새로운 SQL 및 환경변수 없음

# CHANGELOG

## V9.8.1 - 클라이언트 세션 및 홈 진입 최적화
### 추가
- Supabase 세션 결과 15초 메모리 캐시
- 동시에 발생한 세션 조회가 하나의 Promise를 공유하는 중복 요청 차단
- 로그인·로그아웃 상태 변경 시 세션 캐시 자동 갱신
- 보호자 홈 계정 역할 사용자별 5분 세션 캐시

### 수정
- 홈 돌봄 요약, 자주 가는 병원, 하단 채팅 배지가 공용 세션 조회를 사용
- 홈 재진입 때 관리자·병원·프로필 3개 테이블을 매번 재조회하던 동작 축소
- 경로 prefetch를 주요 5개 경로와 보조 2개 경로로 나누어 순차 실행
- 데이터 절약 모드와 2G 네트워크에서는 자동 prefetch 중지

### 오류 수정
- 홈 진입 시 여러 컴포넌트가 동시에 `auth.getSession()`을 호출하며 발생하던 불필요한 대기와 저장소 접근 감소
- 앱 시작 직후 7개 경로를 한꺼번에 prefetch해 저속 네트워크에서 초기 화면을 방해할 수 있던 문제 완화

### 기존 기능 영향
- 채팅 메시지 수신, FCM 푸시, push_jobs, 예약, 병원 DB는 변경하지 않음
- 새로운 SQL 및 환경변수 없음

## V9.7.0 - 내구성 있는 푸시 발송 구조
### 추가
- `push_jobs` 비동기 FCM 발송 큐
- 병원 발신 `chat_messages INSERT` 자동 감지 DB trigger
- 발송 작업 중복 방지 키
- 최대 5회 지수형 재시도와 실패 사유 기록
- Supabase Edge Function `process-push-jobs`
- 만료·해지 FCM 토큰 자동 비활성화
- notification+data 혼합 FCM payload 및 Web Push high urgency

### 수정
- 채팅 API 내부의 직접 FCM 발송 제거
- 푸시 발송을 채팅 HTTP 요청과 완전히 분리
- Service Worker가 notification payload를 다시 표시하지 않도록 중복 방지
- PWA 캐시 버전을 `pawu-shell-v9.7.0`으로 갱신

### 오류 수정
- 메시지는 저장되지만 Vercel 요청 종료·네트워크 상태에 따라 푸시가 누락될 수 있던 문제
- 앱 종료·다른 화면·잠금 상태에서 발송 신뢰도가 일정하지 않던 문제
- 발송 실패 원인과 재시도 상태를 확인할 수 없던 문제

### 기존 기능 영향
- 채팅 저장, 채팅 목록, Realtime 수신, 앱 내부 알림은 유지
- 보호자→병원 메시지에는 시스템 푸시 작업을 만들지 않음
- 병원→보호자 메시지만 사용자 알림 설정을 확인해 작업 생성
- Edge Function/Webhook 설정 전에는 새 큐가 생성돼도 자동 처리되지 않음

## V9.6.4 - 보호자 채팅 수신 핫픽스
### 수정
- 모바일 PWA에서 Realtime 이벤트가 누락될 때 마지막 메시지 이후만 가볍게 보조 동기화

## V9.6.3 - 성능 안정화
### 수정
- 채팅 및 병원 화면 반복 전체 조회 축소
- 백그라운드 불필요 조회 중지

## V9.6.2 - FCM 안정화
### 수정
- Firebase 설정 진단 및 Service Worker 통합
- 만료 토큰 비활성화


## V9.7.1 푸시 토큰 등록 진단
- 알림 권한과 FCM 토큰 DB 등록 상태를 분리해 표시한다.
- `/api/push/register` GET으로 현재 사용자의 활성 토큰 존재 여부를 확인한다.
- 토큰 저장 성공 후 화면 상태를 즉시 갱신한다.

## V9.8.0 - 출시 체감 속도 및 전환 UX
### 추가
- 주요 보호자 경로 유휴 시간 사전 로딩(prefetch)
- 전역 경로 전환 스켈레톤 화면
- 모바일·저감 모션 환경에 맞춘 로딩 UI

### 수정
- 자주 가는 병원 홈 카드에 60초 세션 캐시 적용
- 자주 가는 병원 조회에서 원격 사용자 재검증 대신 기존 세션 우선 사용
- 홈 돌봄 요약과 자주 가는 병원의 중복 동시 요청 차단
- 화면 복귀 시 캐시가 유효하면 서버를 다시 조회하지 않도록 조정

### 기존 기능 영향
- 채팅, 예약, FCM 푸시, Supabase 테이블 및 Edge Function 구조는 변경하지 않음
- 새로운 SQL 및 환경변수 없음

## V9.8.2 - Hospital map performance
- 병원 지도용 10,000여 건 데이터를 10분간 메모리/세션 캐시하도록 변경했습니다.
- 같은 앱 세션에서 병원 찾기 화면 재진입 시 전체 병원 DB 재다운로드를 방지했습니다.
- 동시에 여러 병원 데이터 요청이 발생하면 하나의 요청을 공유합니다.
- 검색 입력에 React deferred value를 적용해 10,000여 건 필터링 중 키 입력 끊김을 줄였습니다.
- 채팅, FCM, 예약 및 병원 데이터 정책에는 영향이 없습니다.

## V9.9.0 - 출시 안정성 및 예외 UX
- 전역 네트워크 연결 상태 배너 추가
- 연결 복구 시 자동 안내 추가
- 라우트 오류 전용 다시 시도·홈 이동 화면 추가
- 앱 최상위 오류 화면 추가
- PAWU 전용 404 화면 추가
- 채팅, FCM 푸시, 예약 및 데이터 구조 영향 없음

## V10.0.0 RC3 Android 실기기 수정
- 실제 렌더링 파일 `app/health-notebook/page.tsx`의 모바일 레이아웃을 수정했습니다.
- 상단 동작 버튼을 3열 고정형으로 변경했습니다.
- 반려동물 선택과 요약 카드를 모바일 2열로 변경했습니다.
- 체중 그래프를 최근 7회 기준, 가로 스크롤 없는 반응형 그래프로 변경했습니다.
- 최근 최저·현재·최근 최고 체중을 함께 표시합니다.
- 건강 화면 및 이벤트 입력·수정 화면에서 플로팅 채팅 버튼을 숨깁니다.
- 그 외 화면의 플로팅 채팅 버튼은 원형 아이콘으로 축소했습니다.
- 첨부 업로드 실패 시 이미 저장된 건강 이벤트를 삭제하지 않도록 변경했습니다.
- 첨부 테이블, Storage 버킷, RLS 및 PostgREST 스키마 캐시 복구 SQL을 추가했습니다.


## V12.2.4
- Fix: 채팅 증분 조회 API가 없어 양방향 메시지가 실시간 수신되지 않던 문제 수정
- Fix: 전송 API가 `messageId`만 반환해 Optimistic UI가 동기화되지 않던 문제 수정
- Improve: 채팅 API 응답 캐시 방지


## V13.0.0
- Rebuild: 보호자 푸시 등록 시스템을 Firebase npm SDK 기반으로 전면 재구축
- Fix: 권한 허용 후 연결창이 멈추고 fcm_tokens가 생성되지 않던 문제 대응
- Fix: 중복 서비스 워커 충돌 제거
- Improve: 단계별 상태·오류 표시 및 토큰 초기화 기능
- Improve: 앱 종료 상태 notification payload 발송


## V13.0.1
- Fix: Firebase onMessage Unsubscribe 반환 타입과 cleanup 함수 타입 불일치 수정
- Build: PushNotificationManager cleanup을 optional function으로 안전하게 처리
