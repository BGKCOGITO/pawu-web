# CHANGELOG

## V9.6.3 - 성능 안정화
### 수정
- 채팅방의 5초 간격 전체 대화 재조회를 제거하고 `chat_messages` Realtime INSERT 수신으로 변경
- 메시지 전송 API가 생성된 메시지 전체 정보를 반환하도록 변경
- 메시지 전송 후 전체 채팅방을 다시 불러오지 않고 생성 메시지를 즉시 화면에 추가
- 채팅방 요청 진행 중 동일한 전체 조회가 중복 실행되지 않도록 차단
- 앱이 다시 보이거나 포커스를 얻을 때만 필요한 경우 1회 동기화
- 보호자 하단 채팅 배지의 15초 반복 조회를 제거하고 `notifications` Realtime 및 읽음 이벤트로 갱신
- 병원 채팅 목록의 15초 반복 조회를 제거하고 채팅 메시지/채팅방 변경 시 갱신
- 병원 접수·대기 보드의 15초 반복 조회를 제거하고 `hospital_waiting_entries` 변경 시 갱신
- 짧은 시간에 Realtime 이벤트가 연속 발생할 때 250ms 단위로 합쳐서 한 번만 조회

### 오류 수정
- 메시지 전송 직후 전체 조회와 Realtime INSERT가 겹쳐 동일 메시지가 중복 표시될 가능성 방지
- 화면 복귀 이벤트가 중복 발생해 같은 API를 여러 번 호출할 가능성 방지
- 읽음 처리 실패가 채팅 메시지 표시까지 막을 수 있는 구조 분리

### 기존 기능 영향
- 기존 채팅방 UI, 예약 연결, 보호자·반려동물 정보 패널은 유지
- Firebase 종료 상태 푸시, 알림 소리·진동 및 개인정보 보호 문구 유지
- 기존 Supabase 테이블과 SQL 변경 없음
- 새 환경변수 없음
- 병원 대시보드 60초 갱신과 복약 알림 타이머는 목적이 달라 유지

## V9.6.2 - FCM 안정화
### 추가 및 수정
- Firebase 서비스 계정 JSON 파싱 안정화
- 동적 Firebase Service Worker와 PWA 캐시 통합
- 데이터 전용 FCM 메시지 및 중복 알림 방지
- Vercel Firebase 환경변수 8개 등록 후 앱 종료 상태 푸시 정상 작동 확인

### 기존 기능 영향
- 채팅 저장 실패와 푸시 실패를 분리
- 메시지 본문은 푸시에서 노출하지 않음

## V9.6.4 - Guardian chat receive hotfix
### Fixed
- 보호자 채팅방에서 병원 발신 메시지가 Realtime 이벤트를 놓치면 화면 조작 전까지 나타나지 않던 문제를 수정했습니다.
- 마지막 메시지 ID 이후의 새 메시지만 가져오는 경량 GET API와 화면 표시 중 보완 동기화를 추가했습니다.

### Impact
- 기존 채팅 저장, FCM 푸시, 예약, 병원 관리자 기능은 변경하지 않았습니다.
- 5초 전체 대화 재조회는 복구하지 않았습니다.
